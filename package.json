{
  "name": "polymorph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ taskudis/Morphs --access-token 6c90fc563d7142078e35d102a166f93a",
    "create-local": "graph create --node http://localhost:8020/ taskudis/Morphs",
    "remove-local": "graph remove --node http://localhost:8020/ taskudis/Morphs",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 taskudis/Morphs"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.0",
    "@graphprotocol/graph-ts": "^0.20.0",
    "@types/node-fetch": "^2.5.10",
    "node-fetch": "^2.6.1"
  }
}
