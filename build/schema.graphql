type TokenMorphedEntity @entity {
  id: ID!
  tokenId: BigInt!
  oldGene: BigInt!
  newGene: BigInt!
  priceForGenomeChange: BigInt!
  eventType: Int!
}

type TransferEntity @entity {
  id: ID!
  from: Bytes!
  to: Bytes!
  tokenId: BigInt!
  tokenURI: String!
}
